import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  // Optional: If you are hosting your site at https://GitUtk.github.io/shadstack-ui
  // rather than a custom domain, uncomment the lines below:
  // basePath: "/shadstack-ui",
  // assetPrefix: "/shadstack-ui/",
};

export default nextConfig;
